# PetClinic Setup: Manual Run and Docker Hub Push

## Overview

This project involves running the PetClinic application manually inside an Ubuntu Docker container and pushing the resulting Docker image to Docker Hub.

